{
  "name": "councilor-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Throwaway demo repo for the Councilor cost spike. Each src/*.ts is an unimplemented stub; each tests/*.test.ts is the spec. Agent implements the stub to make its test pass.",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.8"
  }
}
